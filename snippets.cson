'.source.js':
  'const study':
    'prefix': 'study'
    'body': """
      const study = $1
      Object.keys(study).sort().forEach(key => {
        console.log(
          'key: ' + key,
          study[key]
            ? study[key]
            : 'empty'
        )
      })
    """

  'it ("should...':
    'prefix': 'it'
    'body': """
      it('should ${1}', () => {
        $2
      })
    """
